name: safe-gh-run-view

on: push

permissions: {}

jobs:
  run-ai:
    runs-on: ubuntu-latest
    steps:
      - uses: anthropics/claude-code-action@v1
        with:
          prompt: "Check this run: gh run view ${{ github.run_id }}"
          anthropic_api_key: ${{ secrets.ANTHROPIC_API_KEY }}
