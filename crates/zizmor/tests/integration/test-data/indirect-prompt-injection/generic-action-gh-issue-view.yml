name: generic-action-gh-issue-view

on: issue_comment

permissions: {}

jobs:
  run-ai:
    runs-on: ubuntu-latest
    steps:
      - uses: some-org/unknown-ai-action@v1
        with:
          prompt: |
            Run gh issue view ${{ github.event.issue.number }} and summarize it.
