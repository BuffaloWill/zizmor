name: tag-pinned

on: push

permissions: {}

jobs:
  run-ai:
    runs-on: ubuntu-latest
    steps:
      - uses: google-gemini/gemini-cli-action@v0.2.1
        with:
          prompt: "Review this PR"
          google_ai_studio_key: fake-key
