name: sha-pinned

on: push

permissions: {}

jobs:
  run-ai:
    runs-on: ubuntu-latest
    steps:
      - uses: google-github-actions/run-gemini-cli@abcdef1234567890abcdef1234567890abcdef12
        with:
          prompt: "Review this PR"
          gemini_api_key: fake-key
