name: settings-expression

on: push

permissions: {}

jobs:
  run-ai:
    runs-on: ubuntu-latest
    steps:
      - uses: google-github-actions/run-gemini-cli@v1
        with:
          prompt: "Review this PR"
          gemini_api_key: fake-key
          settings: ${{ vars.GEMINI_SETTINGS }}
