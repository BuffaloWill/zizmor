name: gemini-cli-arbitrary

on: issue_comment

permissions: {}

jobs:
  run-ai:
    runs-on: ubuntu-latest
    steps:
      - uses: google-github-actions/run-gemini-cli@v1
        with:
          prompt: "${{ github.event.comment.body }}"
          gemini_api_key: fake-key
