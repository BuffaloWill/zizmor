name: generic-prompt-arbitrary

on: issue_comment

permissions: {}

jobs:
  run-ai:
    runs-on: ubuntu-latest
    steps:
      - uses: some-org/unknown-ai-action@v1
        with:
          prompt: "${{ github.event.issue.body }}"
