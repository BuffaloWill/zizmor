name: claude-safe-context

on: push

permissions: {}

jobs:
  run-ai:
    runs-on: ubuntu-latest
    steps:
      - uses: anthropics/claude-code-action@v1
        with:
          prompt: "Review commit ${{ github.sha }}"
          anthropic_api_key: ${{ secrets.ANTHROPIC_API_KEY }}
